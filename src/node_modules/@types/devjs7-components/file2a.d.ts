module "devjs7-components/react-regulator" {
    namespace ReactRegulatorNS {
        interface ReactRegulatorProps {
            onChange(val: number): void;

            percentageNumber: number;
        }
    }
}
